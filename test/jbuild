(jbuild_version 1)

(executables
 ((names (test_xlsx_parser))
  (libraries (csv oUnit xlsx_parser))
	(preprocess (pps (ppx_jane ppx_driver.runner)))))

(alias
 ((name runtest)
  (deps (test_xlsx_parser.exe))
 (action
  (chdir ${ROOT}/../..
   (run ${<})))))
