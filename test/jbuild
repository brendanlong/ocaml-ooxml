(jbuild_version 1)

(executables
 ((names (test_easy_xlsx_docs))
  (libraries (csv easy_xlsx oUnit))
	(preprocess (pps (ppx_jane ppx_driver.runner)))))

(alias
 ((name runtest)
  (deps (test_easy_xlsx_docs.exe))
 (action
  (chdir ${ROOT}/../..
   (run ${<})))))
