(jbuild_version 1)

(library
 ((name open_packaging)
  (public_name open_packaging)
  (libraries (base core_kernel stdint utils xml-light))
  (flags (:standard -w -47))
  (preprocess (pps (bisect_ppx -conditional ppx_jane ppx_driver.runner)))))
