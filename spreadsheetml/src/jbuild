(jbuild_version 1)

(library
 ((name spreadsheetml)
  (public_name spreadsheetml)
  (libraries (open_packaging))
  (flags (:standard -w -47))
  (preprocess (pps (bisect_ppx -conditional ppx_jane)))))
